# EconoLens — 実体経済駆動型 株価予測・シミュレーションツール 設計プラン

作成日: 2026-06-12
ステータス: 設計フェーズ(実装前)

---

## 1. コンセプト

過去の株価チャートだけから予測する従来型ツールとは異なり、**「世の中で起きている出来事」→「経済・地域・人への波及」→「需要の発生」→「企業業績への影響」→「株価への影響」という因果連鎖を LLM(Claude)で推論し、定量モデル(モンテカルロ・シミュレーション)と統合して株価を予測する**ツール。

### 代表ユースケース: TSMC 熊本進出(JASM)

```
イベント: TSMC が熊本に半導体工場を建設(JASM 第1・第2工場、投資3兆円超)
  │
  ├─ 1次波及(直接需要)
  │    ├─ 工場建設・インフラ工事     → 九電工(1959)、地場ゼネコン
  │    ├─ 半導体製造装置・搬送装置   → 東京エレクトロン(8035)、平田機工(6258)
  │    └─ 半導体部材・リードフレーム → 三井ハイテック(6966)、信越化学(4063)
  │
  ├─ 2次波及(人と地域の変化)
  │    ├─ 雇用 3,400人超 + 台湾からの移住 → 人口流入
  │    ├─ 住宅需要        → Lib Work(1431)、地場不動産
  │    ├─ 金融・ローン需要 → 九州FG(7180、肥後銀行)
  │    └─ 交通・小売・教育 → JR九州(9142)、地域サービス業
  │
  └─ 3次波及(構造変化)
       ├─ 地価・賃金相場の上昇 → 地元中小企業の人件費圧迫(マイナス影響)
       ├─ 関連企業の九州集積(ソニーCMOS増強など) → 「九州シリコンアイランド」テーマ
       └─ 第3工場・先端パッケージ工場の観測 → 期待の先行織り込み
```

この因果連鎖の各ノードに対して「方向(+/-)・強度・波及時期・確信度」をスコア化し、
銘柄ごとの価格シミュレーションに注入する。

---

## 2. 機能要件

| ID | 機能 | 説明 |
|----|------|------|
| F1 | **推奨銘柄ランキング** `recommend` | 現在アクティブなイベント群を集計し、「今、買い」「今、売り」の上位 N 銘柄を根拠付きで提示 |
| F2 | **個別銘柄予測** `forecast` | 指定銘柄が「任意の期間後」(1ヶ月〜5年)にどんな価格になっているかを確率分布(P10/P50/P90)+シナリオ別で予測 |
| F3 | **イベント・シミュレーション** `simulate` | 「TSMC が熊本に第3工場を建てたら」のような実在/仮想イベントの what-if 分析 |
| F4 | **マルチ市場対応** | 日本株・米国株・中国株(上海/深圳/香港)・指数(S&P500、日経225、ACWI)・ETF・投資信託(オルカン等) |
| F5 | **レポート出力** | Markdown / HTML レポート + チャート(因果連鎖図、価格分布ファンチャート) |
| F6 | **バックテスト** | 過去のイベント(TSMC進出発表 2021-11 など)を使い予測精度を検証 |

### 非機能要件

- 個人の調査・研究用途(投資助言業ではない。出力は常に免責文言付き)
- 無料データソース中心で動作する(API キーは Anthropic のみ必須)
- LLM コストは 1 回のフルスキャンで数ドル以内を目安に設計

---

## 3. 全体アーキテクチャ(5層)

```
┌────────────────────────────────────────────────────────────┐
│ ① データ収集層 (Ingestion)                                  │
│   株価/指数/ETF: yfinance ─ ニュース: GDELT・RSS            │
│   マクロ: FRED・e-Stat ─ 開示: EDINET・SEC EDGAR            │
└──────────────────────────┬─────────────────────────────────┘
                           ▼
┌────────────────────────────────────────────────────────────┐
│ ② イベント・知識分析層 (LLM = Claude)                       │
│   ニュース構造化(Haiku) → 因果連鎖推論(Fable/Opus)          │
│   → 銘柄マッピング → インパクトスコア → ナレッジグラフ蓄積  │
└──────────────────────────┬─────────────────────────────────┘
                           ▼
┌────────────────────────────────────────────────────────────┐
│ ③ 定量予測層 (Quant)                                        │
│   ヒストリカル統計(ドリフト/ボラ/β/為替感応度)              │
│   + バリュエーション Z スコア → GBM モンテカルロ基盤        │
└──────────────────────────┬─────────────────────────────────┘
                           ▼
┌────────────────────────────────────────────────────────────┐
│ ④ 統合・シナリオ層 (Fusion)                                 │
│   インパクトスコアをドリフト/ボラ調整に変換して注入          │
│   強気/中立/弱気シナリオ × 確率 → 統合価格分布 → 推奨度     │
└──────────────────────────┬─────────────────────────────────┘
                           ▼
┌────────────────────────────────────────────────────────────┐
│ ⑤ 出力層 (CLI → 将来 Web UI)                                │
│   recommend / forecast / simulate / report / backtest      │
└────────────────────────────────────────────────────────────┘
```

---

## 4. 各層の設計

### 4.1 ① データ収集層

| データ | ソース | 備考 |
|--------|--------|------|
| 株価・指数・ETF(日米中) | **yfinance** | 日本株 `7203.T`、上海 `600519.SS`、深圳 `000001.SZ`、香港 `0700.HK`、指数 `^GSPC` `^N225`、ETF `ACWI` `SPY` 等。無料・非公式 API(個人利用) |
| 投資信託(オルカン等) | 三菱UFJ AM 公開 CSV、または **ACWI×USDJPY をプロキシ** | オルカンは ACWI 連動なので為替調整プロキシで実用上十分。基準価額は 1 営業日遅延 |
| ニュース(世界・多言語) | **GDELT 2.0**(無料)、RSS(NHK・日経・Reuters・新華社系) | 日本語・英語・中国語をカバー。中国語ソースは信頼度を別重み付け |
| マクロ指標 | **FRED**(米・無料 API)、e-Stat(日)、世界銀行 | 金利・CPI・為替・半導体出荷統計など |
| 企業開示 | EDINET API(日)、SEC EDGAR(米)、TDnet | 有報・決算短信。Phase 2 以降で利用 |
| 為替 | yfinance(`USDJPY=X` 等) | 為替感応度の計算に使用 |

- 取得データは SQLite + Parquet キャッシュに保存し、再取得を最小化。
- 銘柄マスタ(`instruments`)は日本株主要 500 + S&P500 構成銘柄 + 中国主要 100 + 主要 ETF/指数 50 を初期収録。地域タグ(例: `熊本`, `九州`)・テーマタグ(例: `半導体`, `地銀`)を持たせる。

### 4.2 ② イベント・知識分析層(本ツールの核)

LLM を 2 段構えで使い、コストと精度を両立する:

1. **一次フィルタ・構造化(claude-haiku-4-5)** — 大量のニュースを安価に処理
   - 入力: ニュース見出し+本文
   - 出力: `{イベント種別, 主体, 地域, 規模(金額/人数), 期間, 市場関連度スコア}`
   - 関連度の低いニュースをここで足切り
2. **因果連鎖推論(claude-fable-5)** — 上位イベントのみ深掘り
   - プロンプトで「地域経済への波及」「サプライチェーン上流/下流」「労働市場・人口動態」「新規需要の創出」「負の影響(競合・コスト上昇)」の 5 観点からの推論を強制
   - 出力(JSON): 因果連鎖ツリー + 銘柄ごとのインパクトスコア

```jsonc
// インパクトスコアのスキーマ
{
  "event_id": "evt_2026_jasm_fab3",
  "instrument": "1959.T",          // 銘柄マスタと突合して検証(ハルシネーション対策)
  "direction": 1,                   // +1 / -1
  "magnitude": 0.6,                 // 0–1: 業績への影響の大きさ
  "horizon_months": 12,             // 効果が顕在化するまでの期間
  "confidence": 0.7,                // 0–1: 推論の確信度
  "already_priced_in": 0.5,         // 0–1: 既に株価に織り込み済みと推定される度合い
  "rationale": "第3工場の電気設備工事を受注する蓋然性が高い。過去2工場で実績あり。",
  "evidence": ["GDELT:...", "LLM推論"]  // 出典 or 「LLM推論」を明示
}
```

   - **織り込み済み度(`already_priced_in`)が重要**: イベント発表直後に株価が急騰済みなら、いまから買う妙味は小さい。発表日からの株価反応を定量側から LLM にフィードバックして補正する。
3. **ナレッジグラフ蓄積** — 企業・地域・テーマ・サプライチェーン関係を SQLite にグラフとして蓄積し、次回以降の推論で再利用(毎回ゼロから推論しない)。

### 4.3 ③ 定量予測層

- **ベースライン**: 幾何ブラウン運動(GBM)によるモンテカルロ・シミュレーション(10,000 パス)
  - ドリフト μ: 過去 3 年の対数リターン平均(市場全体リターンで縮約してノイズ低減)
  - ボラティリティ σ: 過去 1 年のヒストリカルボラ(EWMA)
- **銘柄特性**: 市場 β、為替感応度(対 USDJPY 回帰)、セクターモメンタム、バリュエーション Z スコア(PER/PBR のセクター内偏差)
- 指数・ETF・投信は個別イベントの影響を「構成国・セクターのマクロイベント」経由で受ける設計(例: 米利下げ → S&P500 / オルカンのドリフト調整)

### 4.4 ④ 統合・シナリオ層

1. アクティブな全イベントのインパクトスコアを銘柄ごとに集計し、**年率アルファに変換**してドリフトに加算:
   `α = Σ direction × magnitude × confidence × (1 − already_priced_in) × decay(horizon)`
   不確実性が増す分、ボラティリティにも上乗せする。
2. **シナリオ 3 本**を生成し、LLM が各シナリオの発生確率を見積もる:
   - 強気: イベント効果がフルに発現(例: 第3工場決定+前倒し)
   - 中立: 現在の計画通り
   - 弱気: 効果剥落+リスク顕在化(例: 半導体市況悪化、地政学リスク)
3. シナリオ別モンテカルロを確率加重して**統合価格分布**を出力: 期待値、P10/P50/P90、元本割れ確率。
4. **推奨度スコア** = 期待リターン(P50)÷ 下方リスク(P10 ドローダウン)× 確信度 → 「強い買い/買い/中立/売り/強い売り」。

### 4.5 ⑤ 出力層

CLI(typer + rich)を先行、Streamlit ダッシュボードを Phase 6 で追加:

```bash
# 今のおすすめ(買い/売り)を根拠付きで
econolens recommend --market jp --top 10
econolens recommend --market all --side sell

# 個別銘柄の任意期間後の価格予測
econolens forecast 6258.T --horizon 1y      # 平田機工、1年後
econolens forecast AAPL --horizon 6m
econolens forecast ACWI --horizon 3y        # オルカン相当

# what-if イベントシミュレーション
econolens simulate "TSMCが熊本に第3工場建設を決定" --horizon 3y --market jp

# ニュース取り込み+イベント分析の更新(cron 運用想定)
econolens ingest --since 7d

# HTML レポート(ファンチャート+因果連鎖図)
econolens report 6258.T --output report.html

# バックテスト
econolens backtest --event "JASM進出発表" --as-of 2021-11-09
```

---

## 5. データモデル(SQLite 主要テーブル)

| テーブル | 内容 |
|---------|------|
| `instruments` | 銘柄マスタ: code, market, name, sector, region_tags, theme_tags, currency |
| `prices` | 日足 OHLCV(Parquet キャッシュ併用) |
| `news` | 収集ニュース: source, url, published_at, lang, raw_text, relevance |
| `events` | 構造化イベント: 種別, 主体, 地域, 規模, 期間, status(active/expired) |
| `impact_scores` | イベント×銘柄の影響スコア(4.2 のスキーマ) |
| `knowledge_edges` | ナレッジグラフ: (企業/地域/テーマ) 間の関係と根拠 |
| `forecasts` | 予測実行履歴: 実行時点の入力・分布を保存 → 後日答え合わせ |
| `recommendations` | 推奨履歴(同上) |

`forecasts` / `recommendations` に実行時スナップショットを残すのは、**自前の予測精度をあとから検証できるようにする**ため(F6 バックテストの土台)。

---

## 6. 技術スタック

| 領域 | 選定 | 理由 |
|------|------|------|
| 言語 | Python 3.12 + **uv** | データ系ライブラリの充実、uv で再現性ある環境 |
| CLI | typer + rich | サブコマンド構成と見やすいテーブル/色出力 |
| データ取得 | yfinance, requests, feedparser | 全市場を無料でカバー |
| LLM | anthropic SDK(claude-fable-5 / claude-haiku-4-5) | 因果推論は最上位、量物は安価モデルの 2 段構え |
| 数値計算 | numpy, pandas | モンテカルロ、統計 |
| 保存 | SQLite + Parquet | サーバ不要、単一ファイル |
| 可視化 | plotly(HTML 出力)、rich(端末) | ファンチャート・因果連鎖図 |
| 将来 UI | Streamlit → (本格化時)FastAPI + React | 最短でダッシュボード化 |

必要な環境変数: `ANTHROPIC_API_KEY`(必須)、`FRED_API_KEY`(無料登録、Phase 2)

---

## 7. 開発フェーズ(Claude Code への依頼単位)

各フェーズが独立して動作確認できる粒度に分割。「Phase N を実装して」とそのまま依頼できる。

| Phase | 内容 | 完了条件(動くもの) | 目安 |
|-------|------|---------------------|------|
| **0** | プロジェクト雛形: uv 環境、設定管理、銘柄マスタ(日米中+ETF/指数)、SQLite スキーマ、CLAUDE.md | `econolens instruments list` が動く | 0.5日 |
| **1** | 価格取得+定量ベースライン: yfinance 取り込み、GBM モンテカルロ、ファンチャート | `econolens forecast 7203.T --horizon 1y` が分布を出す | 1日 |
| **2** | ニュース収集+LLM イベント分析: GDELT/RSS 取り込み、Haiku 構造化、Fable 因果連鎖+インパクトスコア | `econolens ingest` でイベントと影響銘柄が DB に入る | 1–2日 |
| **3** | 統合シナリオ予測+推奨: スコア→ドリフト注入、シナリオ 3 本、`recommend` ランキング | `econolens recommend --market jp` が根拠付きで出る | 1日 |
| **4** | what-if シミュレーション: 自然文イベント入力→因果連鎖→影響銘柄→分布 | TSMC 第3工場の例が end-to-end で動く | 1日 |
| **5** | バックテスト+精度評価: as-of 制約付き再現、方向一致率・P10-P90 カバレッジ | JASM 発表(2021-11)の検証レポートが出る | 1日 |
| **6** | Streamlit ダッシュボード+HTML レポート | ブラウザで recommend / forecast / 因果連鎖図が見られる | 1–2日 |

MVP は **Phase 0–3**(これで「おすすめ提示」と「個別予測」の 2 大機能が揃う)。

---

## 8. 精度評価・バックテスト設計(Phase 5)

- **検証イベント例**: JASM 進出発表(2021-11)、JASM 第2工場発表(2024-02)、日銀マイナス金利解除(2024-03)、米中半導体規制強化、COVID ショック
- **as-of 制約**: 評価時点以降のニュース・価格をパイプラインに入れない
- **既知の限界 — LLM の未来知識リーク**: Claude は学習データとして「その後どうなったか」を知っているため、過去イベントのバックテストは精度を過大評価しやすい。プロンプトで「あなたは YYYY-MM 時点にいる」と制約しても完全には防げない。→ **真の評価は、運用開始後に `forecasts` テーブルへ蓄積した予測の答え合わせ(前向き検証)を主とする**
- 評価指標: 方向一致率(上がる/下がる)、P10–P90 区間の実績カバレッジ(理想 80%)、推奨ポートフォリオの対 TOPIX/S&P500 超過リターン

---

## 9. リスク・制約・免責

1. **株価予測は本質的に不確実**。本ツールの出力は「情報に基づく仮説生成とシミュレーション」であり、投資判断・結果は利用者の自己責任。全出力に免責文言を付す。
2. **法令**: 投資助言・代理業(金商法 28 条)の登録なく第三者へ推奨を提供しないこと。個人利用前提。
3. **データ規約**: yfinance は Yahoo 非公式 API(商用利用不可)。GDELT・FRED・EDINET は公式無料 API。スクレイピングは行わない。
4. **LLM コスト概算**: ニュース 500 本/日 × Haiku 構造化 + 上位 30 イベント × Fable 深掘り ≒ 数ドル/日のオーダー。ナレッジグラフ再利用とプロンプトキャッシュで逓減。
5. **中国株**: 情報統制・会計品質の差を考慮し、中国語ソース由来のイベントは confidence に減衰係数を掛ける。
6. **織り込み済み問題**: 「良いニュース=買い」ではない(発表時に急騰済みなら遅い)。`already_priced_in` 補正が本ツールの精度を左右する最重要パラメータ。
7. **投信の制約**: オルカン等は基準価額が 1 営業日遅延。ACWI プロキシで代替するが乖離(信託報酬・為替ヘッジ差)は明記する。

---

## 10. 将来拡張(スコープ外、メモ)

- ポートフォリオ最適化(推奨銘柄の配分提案)
- アラート機能(新イベント検知時に通知)
- セクターローテーション分析、商品(コモディティ)・債券・為替への拡張
- ナレッジグラフの可視化エクスプローラ
